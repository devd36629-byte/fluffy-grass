<section class="welcome" aria-labelledby="welcome-title">
	<p class="eyebrow">Endless Summer theme component</p>
	<h2 id="welcome-title">This project uses the shared Endless Summer Simulator palette.</h2>
	<p>
		The centralized theme colors live in <code>Layout.astro</code> and are consumed through CSS
		variables across components.
	</p>
</section>

<style>
	.welcome {
		max-width: 720px;
		margin: 48px auto;
		padding: 32px;
		border: 1px solid var(--color-border);
		border-radius: 32px;
		background: var(--color-surface);
		color: var(--color-ink);
		box-shadow: var(--shadow-soft);
	}

	.eyebrow {
		margin: 0 0 12px;
		color: var(--color-accent);
		font-weight: 900;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		font-size: 0.78rem;
	}

	h2 {
		margin: 0;
		font-size: clamp(2rem, 5vw, 4rem);
		line-height: 0.95;
		letter-spacing: -0.07em;
	}

	p:not(.eyebrow) {
		margin: 18px 0 0;
		color: var(--color-ink-muted);
		line-height: 1.7;
	}

	code {
		padding: 0.18em 0.4em;
		border-radius: 8px;
		background: var(--color-accent-soft);
		color: var(--color-ink);
		font-weight: 800;
	}
</style>
